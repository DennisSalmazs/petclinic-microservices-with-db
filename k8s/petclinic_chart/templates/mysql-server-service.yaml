apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yml -o petclinic_chart/templates
    kompose.version: 1.37.0 (fb0539e64)
  labels:
    io.kompose.service: mysql-server
  name: mysql-server
spec:
  # ports:
  #   - name: "3306"
  #     port: 3306
  #     targetPort: 3306
  # selector:
  #   io.kompose.service: mysql-server
  type: ExternalName
  externalName: petclinic.cviyxtix3him.us-east-1.rds.amazonaws.com # Change this line with the endpoint of your RDS.
